# Names and public values only. Never commit values from a live environment.
COMPASS_API_URL=https://your-compass-host.example
COMPASS_API_KEY=
SOLANA_CLUSTER=devnet
TRANSFER_RECIPIENT=
DEMO_RECIPIENT_ALLOWLIST=
TRANSFER_AMOUNT_SOL=0.0005
# Fixed policy value, not a live quote. The runner sends this as a JSON number.
AMOUNT_USD_POLICY_INPUT=0.10
CONFIRMED_TRANSFER=yes
